#include <stdio.h>
int main(){
    // character can be a  letter whether capital or small or a symbol and it's memory it takes 1 byte recall int use 4 bytes

    //declaring  a character we use single quotes  not double quotes

    char firstAlphabeticLetter = 'A';

    //printing a character

    printf("%c",firstAlphabeticLetter);

    return 0;
}